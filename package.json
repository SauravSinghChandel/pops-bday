{
  "name": "pops-bday",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prepare:photos": "node scripts/convert-heif.js",
    "dev": "npm run prepare:photos && vite",
    "build": "npm run prepare:photos && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "heic-convert": "^2.1.0",
    "vite": "^5.4.14"
  }
}
